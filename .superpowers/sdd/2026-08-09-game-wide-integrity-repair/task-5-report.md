# Task 5 report — world placement, supports, collision, and composition

Status: **DONE_WITH_CONCERNS**

## Scope and method

Applied the canonical Rendering / Asset Integrity, Environment Placement,
Overlap Prevention, and Visual QA instructions. No art was rebuilt: measured
source/runtime geometry showed that truthful supports, anchors, patrol limits,
and composition metadata could repair the reproduced defects.

## RED → GREEN evidence

1. The original focused matrix was 30/30, proving the existing tests did not
   cover the complete authored world.
2. New full-envelope tests failed at 31/34:
   - Level 1 pickups lacked named supports.
   - Two Level 1 flying actors exceeded their vertical bands after visual
     bounds and hover excursion were included.
   - rolling encounter introductions exceeded the two-group budget.
   Stale Level 2 moth metadata in the first draft test was corrected before
   world data was changed.
3. A stricter authored-range assertion then failed with twelve real patrol
   overruns: five Level 1 and seven Level 2 ranges allowed the complete largest
   sprite footprint to leave its support or flight band.
4. Checkpoint coverage exposed the Level 1 boss respawn at x=4800 while its
   named ground began at x=4870. It now respawns at x=4900 after a trigger at
   x=4930, preserving the required forward checkpoint ordering.
5. Final focused matrix, including directly affected route suites: **45/45**.

## Relationship ledger

| Relationship | Evidence | Result |
|---|---|---|
| Scenery → named support | full visible rectangles classified against every Level 1/2 surface | PASS |
| Pickup → named support | full visual plus ±2px hover envelope; support width and clearance | PASS |
| Checkpoint → continuous ground | trigger and respawn on named surface; reward Y equals surface Y | PASS |
| Ground actor → support | largest placement footprint at both authored patrol extremes | PASS |
| Flight actor → band | complete horizontal placement footprint and vertical motion envelope | PASS |
| Prop → support | alpha-measured Level 2 environment and boss prop rectangles | PASS |
| Effect → emitter | origin inside body and independent water rectangle; narrow explicit attachment | PASS |
| Boss platform → art/collider/floor | measured opaque top within 2px; base at y=468; collision covered | PASS |
| Runway/arena → transition | minimum runway, purge, clamp/camera, in-bounds boss data, reachable symmetric platforms | PASS |

## Composition ledger

- `completeCompositionItemsForLevel` combines scenery, pickup-hover, and
  encounter-motion footprints with canonical padding.
- 960px viewports sweep each world every 120px.
- Zone boundaries and route reward references are continuous/valid.
- Encounter introduction count never exceeds two per rolling viewport.
- Large encounters are isolated; Level 2's squirrel introduction follows the
  terrier recovery boundary.
- Repeated hero props and overlapping campsite crates are rejected.
- Ordinary patrol envelopes remain clear of each boss runway.

## Authored repairs

- Added semantic support IDs to Level 1 pickups and all checkpoints.
- Grounded Level 1 pigeons on their real surfaces and separated the campsite
  crates.
- Clamped all failing ground/flight patrols using canonical placement
  footprints, and repaired the two Level 1 flight baselines.
- Rebalanced encounter introduction metadata and preserved recovery space.
- Corrected pickup positions to the intended reachable route supports.
- Added grounded Brutus prop metadata; centered the two utility platforms and
  verified normal-jump reach and safe arena spacing.
- Central helpers now resolve full bounds/envelopes, clip requested intervals,
  retain semantic relationships, and return no placement when no candidate is
  legal.

## Browser/runtime evidence

The in-app browser runtime initialized, but navigation to
`http://localhost:3003/trash-dash-alley-acres/?qaRoute=l1-start&build=task5-placement`
did not complete and the call was abandoned. Per the task instruction not to
block on unavailable control, the following are precisely **CANNOT VERIFY**:

- full-speed normal traversal of both worlds;
- direct visual-catalog routes for every level chapter and both arenas;
- foreground occlusion and readable transition observation;
- repaired-defect screenshots and browser warning/error logs.

No runtime screenshot is claimed. Deterministic coverage is not presented as a
substitute for this remaining visual QA.

## Verification

- Focused Task 5 + route matrix: **45/45 PASS**.
- `npm run validate:skills`: **PASS**, 7 canonical skills/references.
- `npm run lint`: **PASS with one pre-existing `<img>` warning**, 0 errors.
- `npm test`: **270/270 PASS** after build.
- `npm run build:pages && npm run test:pages`: **PASS**, Pages 1/1.
- `git diff --check`: **PASS**.
- Generated bytes changed: **none**; deterministic asset rebuild comparison
  not applicable.

## Files

- `app/world-placement.mjs`
- `app/world-scenery.mjs`
- `app/level-one.mjs`
- `app/level-two.mjs`
- `app/boss-arena.mjs`
- `tests/world-placement.test.mjs`
- `tests/world-composition.test.mjs`
- `tests/visual-spawn-envelope.test.mjs`
- `tests/enemy-surface.test.mjs`
- `tests/boss-arena.test.mjs`
- `tests/level-one-routes.test.mjs`
- `docs/visual-audit.md`
- this report

## Self-review and concerns

- Placement semantics and measured geometry are centralized; authored data is
  only narrowed after the central contract rejects an illegal full envelope.
- The original pass did not change runtime rendering; the review repair below
  subsequently wires the validated placement and scenery contracts into the
  production runtime without changing generated art or animation bytes.
- Encounter count intentionally measures authored introductions, while
  expanded patrol envelopes remain in the rolling window item set for spatial
  analysis. This distinguishes pacing from an actor grazing the viewport edge.
- Concern: runtime visual observation remains unavailable and must be completed
  before audit issues can be declared visually closed in the running game.

## Commit

Scoped commit: `fix: enforce world placement and composition` (this report is
included in that commit; the final hash is recorded in the task handoff).

## Round 1 review repair

The review correctly found that the original scoped commit depended on dirty
Level 2 prop helpers, left the central resolver disconnected from production,
and did not make the rolling sweep relationship-sensitive.

- **C1 clean archive:** the scoped package now includes the minimal canonical
  body/emitter/effect geometry helpers, semantic environment placement fields,
  and every expectation changed by Task 5. A clean archive passes the focused
  matrix at 46/46 and its complete package suite/build at 206/206.
- **I1 runtime:** `TrashDashGame` resolves every inventoried ordinary enemy
  through `resolveEnemyWorldPatrol`, including full-footprint ground/flight
  clamping. Illegal support/band placements return `null`; `createLevelRuntime`
  omits them. Direct encounter routes use the same path. Rendering consumes
  `sceneryForLevel(world.levelId)` and the old global scenery array is removed.
- **I2 composition:** the rolling validator now rejects off-world/unsupported
  pickup envelopes, out-of-world or reversed routes, missing landing targets,
  route rewards outside their route, invalid bypass references/occupancy,
  full-envelope density failures, foreground-scene layers at/above gameplay,
  and overlaps between a large encounter's expanded motion footprint and any
  other encounter. Negative mutation fixtures cover each failure family.
- Authored follow-up: Level 1's highway bypass span now covers both declared
  bypass encounters. Level 2's terrier patrol ends at x=2340, leaving its full
  expanded large footprint clear of the subsequent squirrel introduction.
- Browser evidence remains **CANNOT VERIFY** for the same recorded tool-control
  boundary; no visual PASS has been inferred.
- Final exact-index verification snapshot:
  expanded focused matrix **50/50 PASS**, clean package build/suite **206/206
  PASS**, Pages **1/1 PASS**, and lint **0 errors** with the same pre-existing
  `<img>` warning. The shared worktree's broader suite also passes **274/274**
  after skill validation and build.
